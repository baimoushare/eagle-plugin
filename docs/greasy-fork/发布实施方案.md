# Greasy Fork 首次发布实施方案

## 当前决定

- 首发平台：Greasy Fork。
- 首发对象：\`eagle-web-collector.user.js\`，版本 \`1.1.1\`。
- 作者显示名：\`laobai\`。
- 反馈邮箱：\`www.774466655@qq.com\`。
- 使用许可：允许用户安装和使用；未经 \`laobai\` 书面授权，不允许修改后再发布、重新打包、镜像分发或商业再分发。
- 当前不做：Chrome/Edge 扩展迁移、自动发布、GitHub 同步、脚本大型拆分。

## 重要边界

\`eagle-x-collector.user.js\` 暂不按同一版权声明发布。它当前包含第三方来源说明、\`@require\` 外部 JSZip 和原有 MIT 标识，必须先单独完成来源、许可证和再分发范围核查，不能直接改成“仅安装使用”。

## 首发前检查

1. 保留 \`_release/greasy-fork/pre-publication-2026-08-23/\` 中的发布前备份。
2. 在 Tampermonkey 中用正式文件重新安装，确认元数据显示为 \`laobai\` 和 \`1.1.1\`。
3. 测试 Fab.com：当前页采集、保存到 Eagle、本地下载、文件夹/标签设置。
4. 测试 E-Hentai/ExHentai：当前页扫描、深度扫描、暂停、继续、停止和限频错误提示。
5. 测试 Eagle 未启动、图片请求失败、用户取消和批量任务中途失败。
6. 发布页面必须标记成人内容，并说明目标网站访问权限、版权责任和频率限制。

## Greasy Fork 页面文案

### 简短描述

在 Fab.com、E-Hentai、ExHentai 页面批量采集图片，可保存到 Eagle 或本地下载。

### 详细介绍

本脚本在支持的网站页面中显示采集面板，可按当前页面或深度扫描方式收集图片。结果可以保存到正在运行的 Eagle 素材库，也可以直接下载到本地；支持选择 Eagle 文件夹、标签、暂停、继续和停止任务。

使用前请安装 Tampermonkey。使用“保存到 Eagle”时，请先启动 Eagle 并确认本机 Eagle Web API 可用；选择“本地下载”时不依赖 Eagle。

本脚本只在用户主动操作后执行采集和下载，不包含广告、统计或开发者服务器上报。页面地址、图片地址和必要的素材数据只用于完成当前采集任务；保存到 Eagle 时会请求用户电脑上的 \`localhost:41595\` Eagle 接口。

请仅保存你有权访问和使用的素材，并遵守目标网站的服务条款、访问限制和适用法律。E-Hentai/ExHentai 可能因页面结构、登录状态或访问频率限制而无法获取原图。

### 权限说明

- \`GM_xmlhttpRequest\`：请求跨域图片资源并连接本机 Eagle。
- \`GM_download\`：执行本地文件下载。
- \`GM_getValue\` / \`GM_setValue\`：保存用户选择的模式、文件夹和标签设置。
- \`@connect\`：仅用于支持网站、图片 CDN 和本机 Eagle API；当前通用图片识别会遇到动态图片域名，因此保留通配连接声明。

### 反馈

邮箱：\`www.774466655@qq.com\`

## 发布顺序

1. 完成本地测试并记录结果。
2. 登录 Greasy Fork，发布“普通用户脚本”。
3. 粘贴 \`eagle-web-collector.user.js\` 的完整内容。
4. 填写名称、描述、详细介绍、版本更新说明、作者和反馈邮箱。
5. 选择“包含成人内容”。
6. 发布后从 Greasy Fork 页面重新安装，不使用本地文件覆盖测试。
7. 记录公开页面地址、发布时间和版本哈希。

## 后续更新规则

工作区脚本是唯一源码。每次更新按以下顺序执行：

\`\`\`text
修改工作区源码
→ 增加 @version
→ 更新 CHANGELOG.md
→ 本地重新安装并测试
→ 更新 Greasy Fork 脚本代码
→ 从公开页面重新安装验证
\`\`\`

首次发布后再考虑 GitHub 同步或自动发布。

